<?xml version="1.0" encoding="utf-8"?>
<!--
  No android:packageNames filter on purpose: we must learn the foreground app
  even when the user switches to a THIRD app, otherwise the read gate would keep
  thinking DJI Fly is still on screen. Model text is only scanned while a DJI
  package is foreground (see DjiFlyAccessibilityService).
-->
<accessibility-service xmlns:android="http://schemas.android.com/apk/res/android"
    android:accessibilityEventTypes="typeWindowStateChanged|typeWindowContentChanged"
    android:accessibilityFeedbackType="feedbackGeneric"
    android:accessibilityFlags="flagRetrieveInteractiveWindows|flagIncludeNotImportantViews"
    android:canRetrieveWindowContent="true"
    android:canTakeScreenshot="true"
    android:description="@string/accessibility_desc"
    android:notificationTimeout="300" />
